<configuration>
    <!-- 콘솔 출력용 Appender 정의 -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg [eventId=%X{eventId}, requestId=%X{requestId}] %n</pattern>
        </encoder>
    </appender>
    <!-- Json 파일 로깅용 (test 프로파일에서는 /var/log 권한이 없는 CI 환경을 고려해
         경로 자체를 안전한 상대경로로 바꾼다. <springProfile>은 <root>/<appender> 내부에
         중첩할 수 없으므로, appender-ref를 조건부로 빼는 대신 경로를 프로퍼티로 분기한다. -->
    <springProperty scope="context" name="LOG_FILE_PATH" source="logging.file.path" defaultValue="/var/log/schedule-backend"/>

    <appender name="FILE_JSON" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_FILE_PATH}/app.log</file>

        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_FILE_PATH}/app.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>7</maxHistory>
        </rollingPolicy>

        <encoder class="net.logstash.logback.encoder.LoggingEventCompositeJsonEncoder">
            <providers>
                <timestamp>
                    <fieldName>timestamp</fieldName>
                </timestamp>
                <pattern>
                    <pattern>
                        {
                        "level": "%level",
                        "thread": "%thread",
                        "logger": "%logger",
                        "message": "%message",
                        "eventId": "%X{eventId}",
                        "requestId": "%X{requestId}",
                        "traceId": "%X{traceId:-}",
                        "spanId": "%X{spanId:-}"
                        }
                    </pattern>
                </pattern>
                <stackTrace>
                    <fieldName>stack_trace</fieldName>
                </stackTrace>
            </providers>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE_JSON"/>
    </root>
</configuration>
